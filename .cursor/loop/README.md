# ループ実行の状態

通常は GitHub issue のタイトルで状態を持つ。

- `[loop-report]` … ユーザーへの報告（判断待ち / リトライ尽き）
- `[loop-halt]` … これ以上自動実行しない

`.cursor/loop/HALT` はこのディレクトリに **停止時だけ** 置くマーカーファイル。**ファイルが存在するだけで halt**（空でも中身があっても同じ）。存在チェックは `-e`。未使用時はこのファイルを置かない。
